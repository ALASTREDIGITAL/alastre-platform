import { AlertTriangle, ArrowRight, CalendarClock, CheckCircle2, ClipboardCheck, MapPinned, MessageSquareText, Search, Users } from "lucide-react";
import { Button } from "@/components/ui/button";
import { DailySeoQueue } from "@/components/local-seo-operations";
const signals=[{label:"Clientes ativos",value:"Sem dados",detail:"Aguardando integração da carteira",icon:Users},{label:"Saúde SEO Local",value:"Sem dados",detail:"Perfis ainda não conectados",icon:MapPinned},{label:"Avaliações",value:"Não conectado",detail:"Respostas pendentes aparecerão aqui",icon:MessageSquareText},{label:"Postagens",value:"Não conectado",detail:"Fila editorial ainda não disponível",icon:CalendarClock}];
export function OverviewModule({firstName,onOpenLocalSeo,onOpenApprovals}:{firstName:string;onOpenLocalSeo:()=>void;onOpenApprovals:()=>void}){return <div className="agency-overview"><section className="ops-welcome"><div><div className="eyebrow"><MapPinned/> CENTRAL DE OPERAÇÕES</div><h1>Boa noite, {firstName}.</h1><p>Acompanhe o que precisa de atenção e conduza a operação local por exceção.</p></div><Button onClick={onOpenLocalSeo}>Abrir SEO Local <ArrowRight/></Button></section><section className="ops-signal-grid" aria-label="Indicadores operacionais">{signals.map(item=>{const Icon=item.icon;return <article key={item.label}><span className="ops-signal-icon"><Icon/></span><div><span>{item.label}</span><strong>{item.value}</strong><small>{item.detail}</small></div></article>})}</section><DailySeoQueue/><section className="ops-main-grid"><article className="panel priority-panel"><div className="panel-heading"><div><span className="section-kicker">ATENÇÃO HOJE</span><h2>Prioridades da operação</h2></div><AlertTriangle/></div><div className="ops-empty"><ClipboardCheck/><strong>Nenhuma prioridade calculada</strong><p>Alertas e oportunidades aparecerão aqui quando os perfis dos clientes estiverem conectados.</p><Button variant="outline" onClick={onOpenLocalSeo}>Preparar SEO Local</Button></div></article><article className="panel ops-queue"><div className="panel-heading"><div><span className="section-kicker">FLUXO HUMANO</span><h2>Aprovações</h2></div><CheckCircle2/></div><div className="queue-summary"><strong>Sem dados</strong><span>Avaliações, postagens e campanhas aguardando decisão serão consolidadas nesta fila.</span></div><Button variant="outline" onClick={onOpenApprovals}>Abrir aprovações <ArrowRight/></Button></article></section><section className="panel connected-work"><div><span className="section-kicker">VISÃO CONSOLIDADA</span><h2>Oportunidades, campanhas e resultados</h2><p>O painel será preenchido por dados reais de SEO Local, sites, mídia e mensuração. Nenhuma métrica foi estimada.</p></div><span><Search/> Aguardando integrações</span></section></div>}
