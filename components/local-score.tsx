import { ChevronDown, CircleHelp, Database, Link2Off } from "lucide-react";
import type { LocalScorePillar, LocalSeoWorkspace } from "@/lib/local-seo-types";
const labels={calculated:"Calculado",partial:"Parcial",insufficient:"Dados insuficientes",not_connected:"Fonte não conectada"}as const;
function Pillar({pillar}:{pillar:LocalScorePillar}){return <details className="local-pillar"><summary><span className="pillar-name"><span className={`pillar-state ${pillar.state}`}/>{pillar.label}</span><span className="pillar-value">{pillar.score??"—"}<ChevronDown/></span></summary><div className="pillar-detail"><div><strong>Sinais analisados</strong>{pillar.signals.length?<ul>{pillar.signals.map(item=><li key={item}>{item}</li>)}</ul>:<p>Nenhum sinal confiável disponível.</p>}</div><div><strong>Recomendação</strong><p>{pillar.recommendation}</p></div></div></details>}
export function LocalScore({workspace,compact=false}:{workspace:LocalSeoWorkspace;compact?:boolean}){return <section className={`panel local-score${compact?" compact":""}`}><div className="local-score-head"><div><span className="section-kicker">ALASTRE LOCAL SCORE · {workspace.score.version}</span><h2>Saúde do posicionamento local</h2></div><span className={`score-status ${workspace.score.state}`}>{labels[workspace.score.state]}</span></div><div className="score-layout"><div className="score-orb"><strong>{workspace.score.value??"—"}</strong><span>de 100</span></div><div className="score-copy">{workspace.connection==="not_connected"?<Link2Off/>:<Database/>}<div><strong>{workspace.score.value===null?"Score ainda não calculado":"Score calculado"}</strong><p>O DNA disponível foi aproveitado, mas a conexão do Perfil Google é necessária para uma leitura confiável.</p></div></div></div>{!compact&&<div className="pillar-list">{workspace.score.pillars.map(pillar=><Pillar key={pillar.key} pillar={pillar}/>)}</div>}<div className="score-method"><CircleHelp/><span>O score só será exibido com evidências suficientes. Cada pilar mantém sinais e recomendações explicáveis.</span></div></section>}
