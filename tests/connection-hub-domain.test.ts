// @ts-expect-error Bun's test types are runtime-provided and not part of the app tsconfig.
import {describe,expect,test} from "bun:test";
import {canBindResource,connectionHubRequest,getClientConnectionContract,googleCloudAdministration,healthCopy,providers,statusCopy} from "../lib/connection-hub-domain";
describe("Connection Hub domain",()=>{test("expõe catálogo sem credenciais",()=>{expect(providers.find(p=>p.key==="google")?.capabilities).toHaveLength(4);expect(providers.find(p=>p.key==="google")?.managementMode).toBe("platform_managed");expect(JSON.stringify(providers)).not.toMatch(/token|secret|api.key/i)});test("registra somente identificadores administrativos públicos",()=>{expect(googleCloudAdministration.projectId).toBe("alastre-platform");expect(googleCloudAdministration.writeMode).toBe("disabled")});test("traduz estados técnicos",()=>{expect(statusCopy.expired.label).toBe("Reconexão necessária");expect(statusCopy.disconnected.label).toBe("Não conectado");expect(healthCopy.provider_pending.label).toBe("Aguardando liberação")});test("bloqueia binding entre tenants",()=>{expect(canBindResource({clientAgencyId:"a",resourceAgencyId:"b",resourceCapability:"google_ads",requestedCapability:"google_ads",resourceActive:true})).toBe(false);expect(canBindResource({clientAgencyId:"a",resourceAgencyId:"a",resourceCapability:"google_ads",requestedCapability:"google_ads",resourceActive:true})).toBe(true)});test("valida contratos",()=>{expect(connectionHubRequest.safeParse({action:"connection_status",connection_id:"x"}).success).toBe(false);expect(getClientConnectionContract("client","google_business_profile").requires_status).toBe("connected")});});
